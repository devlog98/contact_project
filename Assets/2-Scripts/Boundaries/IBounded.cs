/*
 * Group all elements that check Boundaries
 */

namespace devlog98.Boundaries {
    public interface IBounded {
        // execute when element goes outside of boundaries
        void OutOfBounds();
    }
}